[project]
name = "churn-prediction"
version = "0.1.0"
description = "Credit card churn prediction pipeline"
requires-python = ">=3.10"

[tool.black]
line-length = 88
target-version = ["py310"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
